<%= form_for(@user_too) do |f| %>
  <% if @user_too.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user_too.errors.count, "error") %> prohibited this user_too from being saved:</h2>

      <ul>
      <% @user_too.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
